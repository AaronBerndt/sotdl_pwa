import React from "react";
import { Story, Meta } from "@storybook/react/types-6-0";
import {{name}} from "./{{name}}";
export default {
  title: "{{pascalCase componentGroup}}/{{pascalCase componentType}}/{{name}}",
  component: {{name}},
} as Meta;
export const Main: Story = () => <{{name}} sample="test" />;
